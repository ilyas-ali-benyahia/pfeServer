Django==5.1.2
djangorestframework
python-dotenv
gunicorn
whitenoise
django-cors-headers

langchain==0.1.13
langchain-google-genai
google-generativeai
cohere
supabase
langdetect

pytesseract
Pillow
PyPDF2
python-magic  # Use this on Heroku instead of 'python-magic'
youtube-transcript-api
unstructured==0.10.30  # Make sure it's stable and not too new

# Required to fix langchain errors
tiktoken
requests
openai  # Might still be required internally by LangChain

# Compatibility fix for Unstructured
filetype
markdown
